/**
 * @deprecated Diese Datei ist die Legacy-Version des API-Servers (JSON-Flat-File).
 * Sie wird NICHT mehr verwendet. Der aktive Server ist server.js im Root-Verzeichnis
 * mit SQLite-Datenbank (server/database.js).
 *
 * Diese Datei bleibt nur zur Referenz erhalten und kann sicher gelöscht werden.
 */

// --- LEGACY / NICHT AKTIV ---
// Alle aktiven API-Routen befinden sich in: /server.js
// Datenbankschicht: /server/database.js (SQLite via better-sqlite3)
